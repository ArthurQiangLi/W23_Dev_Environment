## OpenCV with Python
Install pip by (you may need install pip first in a new machine):
```
sudo apt install python3-pip
```
You need this below to install in ubuntu.
```
pip install opencv-python
```